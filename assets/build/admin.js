// Compiled admin bundle placeholder. Build your React app to replace this file.
(function(){
    // Placeholder script to avoid console errors until a real build is provided.
    if (typeof window !== 'undefined') {
        window.__infiRewards_BUNDLE = true;
    }
})();
